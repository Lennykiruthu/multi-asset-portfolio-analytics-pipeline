portfolio_analytics:
  target: dev
  outputs:
    dev:
      dbname: "{{ env_var('POSTGRES_DB') }}"
      host: postgres
      password: "{{ env_var('POSTGRES_PASSWORD') }}"
      port: 5432
      schema: bronze
      threads: 1
      type: postgres
      user: "{{ env_var('POSTGRES_USER') }}"

